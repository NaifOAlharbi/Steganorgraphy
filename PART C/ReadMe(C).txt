The user must enter 3 arguments the carrier image ,the message and the name of the result image 
which is the encoded image, with extension of each one of them example:-
Encoder.exe img.png message.png encoded.png
Next, the program will ask the user to input the password.
The resulted image will be saved in the Debug file where the exe is located
